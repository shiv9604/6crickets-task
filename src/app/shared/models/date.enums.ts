export enum WeekDayType {
    long = 'long',
    short = 'short',
    narrow = 'narrow',
  }
  
  export enum YearType {
    numeric = 'numeric',
    twoDigit = '2-digit',
  }
  
  export enum MonthType {
    long = 'long',
    short = 'short',
    narrow = 'narrow',
    twoDigit = '2-digit',
  }
  
  export enum DayType {
    numeric = 'numeric',
    twoDigit = '2-digit',
  }